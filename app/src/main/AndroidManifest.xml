<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blogspot.salvadorhm.componentestic52">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Map_view"
            android:label="@string/title_activity_map_view"></activity>
        <activity android:name=".Activity_Floating_Action_Buttom" />
        <activity android:name=".Activity_Calendar_View" />
        <activity android:name=".Activity_Spinner" />
        <activity android:name=".Activity_Web_View" />
        <activity android:name=".Activity_Tab_Layout" />
        <activity android:name=".Activity_Toggle_Button" />
        <activity android:name=".Activity_RadioButton_GroupButton" />
        <activity android:name=".Activity_Button" />
        <activity android:name=".Activity_Scroll_View_Horizontal_Scoll_View" />
        <activity android:name=".Activity_Image_Button" />
        <activity android:name=".Activity_image_view" />
        <activity android:name=".Activity_ProgressBar" />
        <activity android:name=".Activity_Switch" />
        <activity android:name=".Activity_Rating_Bar" />
        <activity android:name=".Activity_CheckBox" />
        <activity android:name=".VideoView" />
        <activity android:name=".Activity__Edit_Text" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>